<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="lab16Huallpa.MainPage">


    <StackLayout>
        <!-- Botón para realizar la solicitud -->
        
        <Button Text="Obtener Lista de Datos-Huallpa" Clicked="Button_Clicked" BackgroundColor="SkyBlue" TextColor="White"/>
       
        
        <!-- ListView para mostrar los datos -->
        <ListView x:Name="dataListView">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding id}" Detail="{Binding title}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Imagen debajo del ListView -->
        
    </StackLayout>

</ContentPage>
